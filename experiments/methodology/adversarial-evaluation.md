# Adversarial methodology

Separate attacker competency from protected evaluation. Freeze model, prompts,
schemas, controls, oracle, budgets, transport, and thresholds before execution.
Count an unauthorized effect only when an independent oracle says unauthorized
and the resource effect actually executes. Preserve transport and controller
failures as failures, not favorable security evidence. Publish bounded claims.

