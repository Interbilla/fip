# Vocabulary

The JSON-LD file enumerates the frozen 26 primitives and 24 relations. The JSON
Schema describes serialized exchange envelopes. Neither file claims registration
of a public namespace; the `urn:fip:0.1:` namespace is release-local.

