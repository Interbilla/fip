# Governed operational-context projection (GOCP)

GOCP is architecture around FIP, not a canonical FIP primitive. The example
profile admits a fixed set of operational fields and preserves other domain
semantics as ignored description. Active schema and mappings are versioned and
immutable during a run; policy-governed values may vary.

Run `python gocp/project.py` to inspect the synthetic projection.

