# Provenance

Source research repository snapshot:

- branch: `feature/fip-01-preprint-readiness-review`
- commit: `c67930b3836936aca9b12b83f4806c47e3845543`
- source status during extraction: clean
- standalone Python adapter SHA-256:
  `c8bf2a3172d2572bbcc975b1831576a4da24edea59eda8f34f8362a25f19efec`

Intentionally copied without semantic edits:

- Apache-2.0 `LICENSE`
- Python and JavaScript standalone reference adapters (relocated/renamed only)
- 47 conformance vectors and 47 corresponding expected-result files

Newly authored release engineering includes documentation, packaging metadata,
runners, synthetic examples, GOCP demonstration, tests, contribution templates,
and CI. The JSON-LD enumeration and exchange schema reproduce the adapter's
frozen vocabulary/serialized boundary; they do not register an external namespace.

Excluded: internal repository history, application/product code, CAMEO/BILLA
implementations, customer/domain project material, internal experiments and raw
traces, contracts, business material, credentials, private endpoints, and paper
drafts. Only the bounded public evidence summary is included.

The source external release declares Apache-2.0. Copied files retain that project
license. This engineering assessment is not legal advice; a human release owner
should confirm authority and NOTICE attribution before publication.

`docs/source-artifact-hashes.sha256` records copied-file hashes and proves vector
equivalence to the frozen source snapshot.

