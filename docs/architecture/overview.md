# Architecture overview

Domain semantics describe objects and knowledge. GOCP selects operationally
admissible context. FIP describes the consequential interaction. The Semantic
Micro-Firewall evaluates it. A resource gateway alone applies effects.

These are separate layers. FIP does not make domain assertions authoritative.

